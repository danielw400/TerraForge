using System;

namespace TerraForge.Game.Inventory
{
    public sealed class ChestInventory : InventoryContainer
    {
        public ChestInventory(string name, int slotCount) : base(name, slotCount)
        {
        }
    }
}
